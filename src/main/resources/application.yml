spring:
  application:
    name: community-engagement-service

  profiles:
    active: dev
  config:
    import: "configserver:"

  cloud:
    config:
      uri: http://167.172.78.79:8888

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui
